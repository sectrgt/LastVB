Add in comkmand buttonas to put info in infobox 
Info box also cantains description
Recipt (add in the variables to pass index to the command button adding to cart then to sub)*
Fix indexes to correspond with categories(Menu buttons, imgDisplay, img CLick events, udtItems array)-J
Create subroutine to display info
